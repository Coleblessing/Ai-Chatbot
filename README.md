echo "# AI Chatbot
A conversational AI chatbot built with Python and the Groq API using LLaMA 3.

## How to run
1. Clone the repo
2. Add your GROQ_API_KEY to a .env file
3. Run python3 chatbot.py" > README.md
git add README.md
git commit -m "Add README"
git push origin main
